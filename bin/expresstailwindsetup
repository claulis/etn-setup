#!/usr/bin/env node

const colors = require('colors');
const install = require("../lib/index");

const arguments = process.argv.splice(2);

if (arguments.length === 0) {
  console.log(colors.red("Nome do projeto:"));
  process.exit(1);
} else {
  install.copy(arguments[0]);
}

